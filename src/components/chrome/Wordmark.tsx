/**
 * The site wordmark, top-left. Pagkaki set large in the house blue.
 *
 * It is the name, not a logo: no mark, no container, no tagline. The face is
 * doing the work — Pagkaki's uneven, hand-cut letterforms are the personality,
 * so anything built around them would only get in the way.
 */
export function Wordmark() {
  return (
    <a className="chrome__wordmark" href="#top" aria-label="Suman Biswas — home">
      Suman Biswas
    </a>
  )
}
